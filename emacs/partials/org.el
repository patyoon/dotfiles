;; Remove number of words highlighting in org mode.

;; fontify code in code blocks in org mode.
(setq org-src-fontify-natively t)

;; Set up org mobile directory.
(setq org-mobile-directory "~/Dropbox/MobileOrg")

(provide `org)
;;; org.el ends here
